#ifndef __KEETOS_
#define __KEETOS_

// KEEp Track Of Stuff (KEETOS)

#include <string>
#include <vector>

using namespace std;

class Keetos {
    keetos(vector<string> s);
    void run(vector<string> t);
    void to_dos();

}

#endif